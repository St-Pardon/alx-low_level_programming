void *p;

	if (nmemb == 0 || size == 0)
		return (NULL);
	p = malloc(nmemb * size);

	if (p == NULL)
		return (NULL);
	_memset(p, 0, (nmemb * size));
	return (p);#include "main.h"
#include <stdlib.h>

/**
 * _calloc - allocate arr
 * @nmemb: param
 * @size: param
 */

void *_calloc(unsigned int nmemb, unsigned int size)
{
	
