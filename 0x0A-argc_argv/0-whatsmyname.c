#include <stdio.h>

/**
 * main - main func
 * @argc: param count
 * @argv: param pointer
 * Return: int
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
